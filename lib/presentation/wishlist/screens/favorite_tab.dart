import 'package:flutter/material.dart';

class FaviriteTab extends StatelessWidget {
  const FaviriteTab({super.key});

  @override
  Widget build(BuildContext context) {
     return Column(
      mainAxisAlignment: MainAxisAlignment.center,
      children: [Center(child: Text("favorite screen"))],
    );
  }
}